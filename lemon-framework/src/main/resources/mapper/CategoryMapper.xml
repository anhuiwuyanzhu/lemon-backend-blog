<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lemon.violet.dao.CategoryDao">

    <select id="selectNormalList" parameterType="java.lang.Integer" resultType="com.lemon.violet.pojo.entity.Category">
        select a.category_id,c.name,c.pid
        from sg_article a
        inner join sg_category c on a.category_id = c.id
        where a.del_flag = 0 and a.status = '0'
        and c.pid = #{pid}
        group by a.category_id
    </select>
</mapper>
